# Security

This is experimental research software, with no supported stable release line.
Report vulnerabilities through the repository's private vulnerability reporting
feature when available, otherwise contact a maintainer privately on GitHub.
Do not publish credentials or sensitive input data in an issue.

Model serialization uses JSON, not pickle or executable expressions. Review
external experiment scripts before executing them. Numerical validity issues
without a security impact should be reported as ordinary scientific bugs.
